<form method="post" action="{{ path('app_reservhebergement_delete', {'id': reservhebergement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservhebergement.id) }}">
    <button class="btn">Delete</button>
</form>
