{% extends 'base.html.twig' %}

{% block title %}Reservhebergement{% endblock %}

{% block body %}
    <h1>Reservhebergement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservhebergement.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ reservhebergement.username }}</td>
            </tr>
            <tr>
                <th>Id_hebergement</th>
                <td>{{ reservhebergement.idHebergement }}</td>
            </tr>
            <tr>
                <th>Nbr_personne</th>
                <td>{{ reservhebergement.nbrPersonne }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ reservhebergement.duree }}</td>
            </tr>
            <tr>
                <th>Type_paiement</th>
                <td>{{ reservhebergement.typePaiement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservhebergement_index') }}">back to list</a>

    <a href="{{ path('app_reservhebergement_edit', {'id': reservhebergement.id}) }}">edit</a>

    {{ include('reservhebergement/_delete_form.html.twig') }}
{% endblock %}
