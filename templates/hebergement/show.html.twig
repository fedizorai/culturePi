{% extends 'base.html.twig' %}

{% block title %}HEBERGEMENT{% endblock %}

{% block body %}
    <h1>HEBERGEMENT</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hebergement.id_hebergement }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ hebergement.adresse }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ hebergement.type }}</td>
            </tr>
            <tr>
                <th>Nb_chambre</th>
                <td>{{ hebergement.nbChambre }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ hebergement.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hebergement_index') }}">back to list</a>

    <a href="{{ path('app_hebergement_edit', {'id_hebergement': hebergement.id_hebergement}) }}">edit</a>

    {{ include('hebergement/_delete_form.html.twig') }}
{% endblock %}
