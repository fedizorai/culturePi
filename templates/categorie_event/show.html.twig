{% extends './dashboard/base.html.twig' %}

{% block title %}CategorieEvent{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Categorie Event</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th scope="row">Id</th>
                    <td>{{ categorie_event.id }}</td>
                </tr>
                <tr>
                    <th scope="row">Nom</th>
                    <td>{{ categorie_event.nom }}</td>
                </tr>
                <tr>
                    <th scope="row">DescriptionCategorieEvent</th>
                    <td>{{ categorie_event.descriptionCategorieEvent }}</td>
                </tr>
            </tbody>
        </table>

        <div class="d-flex justify-content-center mt-4">
            <a href="{{ path('app_categorie_event_index') }}" class="btn btn-secondary">Back to List</a>
              </div>
                  </div>

<!-- Edit button <a href="{{ path('app_categorie_event_edit', {'id': categorie_event.id}) }}" class="btn btn-warning ms-2">Edit</a> -->
      
    <!-- delete button  {{ include('categorie_event/_delete_form.html.twig') }} -->

{% endblock %}
