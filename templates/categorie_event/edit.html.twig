{% extends './dashboard/base.html.twig' %}

{% block title %}Edit CategorieEvent{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Edit CategorieEvent</h1>

       <div class="card">
    <div class="card-body text-center"> <!-- Added text-center class to center the content -->
        {{ include('categorie_event/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
</div>

        <div class="d-flex justify-content-between align-items-center mt-4">
            <a href="{{ path('app_categorie_event_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left me-2"></i> Back to List
            </a>
            {{ include('categorie_event/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
