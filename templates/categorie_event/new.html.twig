{% extends './dashboard/base.html.twig' %}

{% block title %}New CategorieEvent{% endblock %}
{% block body %}

<div class="container-fluid">
    <div class="card my-4">
        <div class="card-body">
            <h5 class="card-title fw-semibold mb-4">Create New Categorie</h5>
            <div class="card">
                <div class="card-body">
                    {{ include('categorie_event/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>

    <div class="d-flex justify-content-center my-4">
        <a href="{{ path('app_categorie_event_index') }}" class="btn btn-secondary">Back to List</a>
    </div>
</div>

{% endblock %}
