     {% extends 'base1.html.twig' %} 
     {% block body %}
{{ form_start(form , {attr: {'novalidate': 'novalidate' ,'class':'bg-primary'}}) }}
<div class="row">
    <div class="col-lg-12">
                <h4>Make Your <em>Reservation</em> Through This <em>Form</em></h4>
              </div>

              <div class="col-lg-6">
                  <fieldset>
                         {{  form_label ( form.nomEvent,'nom event' ,{ 'attr': {'class': 'form-label'}}) }}
                        {{  form_widget ( form.nomEvent ,{ 'attr': {'class': 'Name'}}) }}

                        {% if form_errors(form.nomEvent) is not empty  %}
                            {{  form_errors( form.nomEvent ,{ 'attr': {'class': 'text-danger'}}) }}
                        {% else %}

                        {% endif %}

                  </fieldset>
                  </div>

<div class="col-lg-6">
                  <fieldset>
                        {{  form_label ( form.maxPlacesEvent,'maxPlacesEvent' ,{ 'attr': {'class': 'form-label'}}) }}
                        {{  form_widget ( form.maxPlacesEvent ,{ 'attr': {'class': 'Name'}}) }}

                        {% if form_errors(form.maxPlacesEvent) is not empty  %}
                            {{  form_errors( form.maxPlacesEvent ,{ 'attr': {'class': 'text-danger'}}) }}
                        {% else %}

                        {% endif %}

                  </fieldset>
                  </div>

<div class="col-lg-6">
                  <fieldset>
                        {{  form_label ( form.dateEvent,'dateEventt' ,{ 'attr': {'class': 'form-label'}}) }}
                        {{  form_widget ( form.dateEvent ,{ 'attr': {'class': 'Name  '}}) }}

                        {% if form_errors(form.dateEvent) is not empty  %}
                            {{  form_errors( form.dateEvent ,{ 'attr': {'class': 'text-danger'}}) }}
                        {% else %}

                        {% endif %}

                  </fieldset>
                  </div>

                  <div class="col-lg-6">

                  <fieldset>
                        {{  form_label ( form.lieuEvent,'lieuEvent' ,{ 'attr': {'class': 'form-label'}}) }}
                        {{  form_widget ( form.lieuEvent ,{ 'attr': {'class': 'Name  '}}) }}

                        {% if form_errors(form.lieuEvent) is not empty  %}
                            {{  form_errors( form.lieuEvent ,{ 'attr': {'class': 'text-danger'}}) }}
                        {% else %}

                        {% endif %}

                  </fieldset>
                  </div>
              </div>
    
    <button class="main-button">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
 {% endblock %}