<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <title>Culture Vibes!</title>
    <!-- Bootstrap core CSS -->
    <link href="{{asset('vendor/bootstrap/css/bootstrap.min.css')}}" rel="stylesheet">
    <!-- Additional CSS Files -->
    <link rel="stylesheet" href="{{asset('css/fontawesome.css')}}">
    <link rel="stylesheet" href="{{asset('css/templatemo-woox-travel.css')}}">
    <link rel="stylesheet" href="{{asset('css/owl.css')}}">
    <link rel="stylesheet" href="{{asset('css/animate.css')}}">
    <link rel="stylesheet" href="https://unpkg.com/swiper@7/swiper-bundle.min.css')}}" />
</head>

<body>

    <header class="header-area header-sticky">
        <div class="container">
        {% for flash_message in app.session.flashBag.get('success') %}
    <div class="alert alert-success">
        {{ flash_message }}
    </div>
{% endfor %}
            <div class="row">
                <div class="col-12">
                    <nav class="main-nav">
                        <a href="index.html" class="logo">
                            <img src="{{ asset('images/logo.png') }}" alt="" style="width: 100px; height: 55px;">
                        </a>
                        <ul class="nav">
                            <li><a href="index.html">Home</a></li>
                            <li><a href="about.html">About</a></li>
                            <li><a href="{{ path('app_voyage_index') }}">Deals</a></li>
                            <li><a href="{{ path('app_reservation_new') }}" class="active">Reservation</a></li>
                            <li><a href="reservation.html">Book Yours</a></li>
                        </ul>
                        <a class='menu-trigger'>
                            <span>Menu</span>
                        </a>
                    </nav>
                </div>
            </div>
        </div>
    </header>

    <div class="second-page-heading">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <h4>Réservez votre offre préférée ici</h4>
                    <h2>Faites votre réservation</h2>
                    <p>Make Your Reservation, Make your reservation, Make your reservation</p>
                </div>
            </div>
        </div>
    </div>

    <div class="search-form">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <form id="search-form" name="gs" method="submit" role="search" action="javascript:void(0);">
                        <div class="row">
                    <div class="section-heading text-center">
                                <h4>List of Reservations</h4>
                                 <div class="row">
        <div class="col-lg-2 mx-auto mt-5 text-center">
            <a href="{{ path('app_reservation_new') }}" class="btn btn-outline-primary m-1"><- Back</a>
        </div>
    </div>
    </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <div class="amazing-deals">
        <div class="container">
            <div class="row">

            </div>
            <div class="row">
                <div class="container">
                    <div class="row">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Id</th>
                                    <th>Nombre De tickets</th>
                                    <th>Your User ID</th>
                                    <th>Paiement Methode</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for reservation in reservations %}
                                    <tr>
                                        <td>{{ reservation.id }}</td>
                                        <td>{{ reservation.nbrtickets }}</td>
                                        <td>{{ reservation.iduser }}</td>
                                        <td>{{ reservation.paiement }}</td>
                                        <td>
                                          <form id="deleteForm" method="post" action="{{ path('app_reservation_deleteFe', {'id': reservation.id}) }}" onsubmit="return confirmAndRefresh('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.id) }}">
    <button class="btn btn-danger m-1">Delete</button>
</form>


                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        <div class="call-to-action">
    <div class="container">
      <div class="row">
        
        <div class="more-info reservation-info">
    <div class="container">
        <div class="row">
            <div class="col-lg-4 col-sm-6">
                <div class="info-item">
                    <i class="fa fa-phone"></i>
        <h4 class="text-black">Passer un appel</h4>
                    <a href="#">+216 58 818 677</a>
                </div>
            </div>
            <div class="col-lg-4 col-sm-6">
                <div class="info-item">
                    <i class="fa fa-envelope"></i>
                    <h4 class="text-black">Nous contacter par courriel</h4>
                    <a href="#">CultureVibes@gmail.com</a>
                </div>
            </div>
            <div class="col-lg-4 col-sm-6">
                <div class="info-item">
                    <i class="fa fa-map-marker"></i>
                    <h4 class="text-black">Visitez nos bureaux</h4>
                    <a href="#">2083 - Pôle Technologique - El Ghazala.</a>
                </div>
            </div>
        </div>
    </div>
      </div>
    </div>
    
  </div>
    </div>
<script>
    function confirmAndRefresh(message) {
        if (confirm(message)) {
            // If confirmed, submit the form and reload the page
            document.getElementById('deleteForm').submit();
            location.reload();
            return true;
        }
        return false;
    }
</script>
			<!-- Scripts -->
			<script src="{{ asset('vendor/jquery/jquery.min.js') }}"></script>
			<script src="{{ asset('vendor/bootstrap/js/bootstrap.min.js') }}"></script>
			<script src="{{ asset('js/isotope.min.js') }}"></script>
			<script src="{{ asset('js/owl-carousel.js') }}"></script>
			<script src="{{ asset('js/wow.js') }}"></script>
			<script src="{{ asset('js/tabs.js') }}"></script>
			<script src="{{ asset('js/popup.js') }}"></script>
			<script src="{{ asset('js/custom.js') }}"></script>
		</body>
       
	</body>
</html>
